# 内容规划首次进入互动规则 V4（历史规则 / SUPERSEDED）

> 状态：**SUPERSEDED，仅保留历史说明，不得驱动当前代码、Prompt、测试或 UI。**
>
> AIA Auto IP 已撤销独立“内容规划”入口和第二轮 onboarding。当前最高事实源为 `docs/product/CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md`；内容方向细节以 `rules/ip-content-strategy-rules.md` 为准。

## 当前有效结论

- 不再播放独立内容规划欢迎卡；
- 不再单独询问专业主线和泛内容候选；
- 不再要求用户点击“生成内容规划”；
- 精简后的内容策略直接进入 IP 方案“内容方向”板块；
- 当前 `primaryGoal` **不能直接照搬历史 `purpose`**：只有明确单一拓客或增员时才可标准化；“拓客和增员都要 / 打造个人品牌 / 打造专业形象 / 兼顾”等必须重新询问并在拓客/增员中二选一；
- 后续目标人群、IP 方案、内容方向、脚本推荐必须读取同一标准化 `primaryGoal`。

## 已替代的历史规则

以下旧表述已经 `SUPERSEDED`：

- “直接读取 IP 档案中的 purpose 作为唯一业务目标来源”；
- “拓客和增员都要”可以直接作为当前目标；
- “打造个人品牌”可以直接作为当前目标；
- 独立内容规划 onboarding / 独立生成方案。

## 历史兼容

旧内容规划 API、历史消息和方案数据可暂时保留以避免破坏已有数据，但不得：

- 决定当前默认导航；
- 覆盖当前 IP 方案内容方向；
- 重新创建独立入口；
- 让旧 `purpose` 覆盖标准化 `primaryGoal`。

如果以后重新引入独立内容规划功能，必须重新经过产品确认，不能因为旧代码仍存在就自动恢复入口。
