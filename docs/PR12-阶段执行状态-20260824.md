# PR #12 阶段执行状态（2026-08-24）

## 当前分支与发布状态

- PR：#12「优化 IP 问题、简介服务与内容主线」
- 开发分支：`agent/ip-product-optimization-20260821`
- 当前仍为 Draft，不合并 main。
- 2026-08-24 当月 GitHub-hosted Actions 额度已耗尽；之后出现的“job 无 steps、直接 failure”按额度阻塞处理，不再连续提交试错。
- Actions 额度耗尽前的 Preview 不能自动代表当前最新开发分支；产品验收必须核对 Preview commit 后再开始。

## 本阶段工作方式调整

从本节点起：

1. 继续在同一开发分支集中处理产品反馈；
2. 日常 commit 不再自动触发完整 Frontend QA；
3. 日常 commit 不再自动部署 Preview；
4. 到自然验收节点后统一更新 Preview；
5. 优先在 ECS 执行阶段相关测试和 Preview 更新；
6. 阶段收口 / 准备合并 main 时再做完整 QA；
7. Actions 恢复额度后也不恢复“每个 PR commit 自动全量跑”的旧方式。

长期规则见：`docs/长期开发规则-低Actions额度与ECS阶段验收.md`。

## 当前产品验收重点

当前仍属于上一轮产品修复验收阶段，不开发新的大功能。重点继续验证：

- 真实报名资料映射；
- 自我介绍语义预处理后是否减少重复询问；
- 客户反馈是否按问卷问题独立展示，而不是重新压回单字段；
- 个人介绍是否在资料最后一整行展示；
- “我的 / 资料”“IP / 方案”两个真实悬浮入口；
- 现有昵称评估是否谨慎、有理有据，并结合学历、荣誉、地域、过往职业等真实特色；
- 脚本改写是否显式代入账号表达风格；
- 脚本改写与小红书排版真实复制按钮是否有明确成功 / 失败反馈；
- 服务型简介是否真正读到报名资料中的真实服务，而不是只存在生成规则却没有数据映射。

## 工程风险提醒

本阶段已经确认出现“后加载脚本覆盖前面修复”的真实问题。后续新增产品修改前，应优先判断是否属于：

- 唯一真源错误；
- 字段标准化 / 数据映射断链；
- 核心函数被多层重写；
- 脚本加载顺序覆盖；
- 测试只验证辅助函数、没有验证最终 DOM。

不允许继续以新增 `product-rules-vXX.js` 作为默认修复方式。当前 `product-integration-v30.js` 是阶段性的最终集成入口；阶段收口后需要安排一次不改变产品行为的前端规则收敛。

## 下一自然节点

继续收集并集中完成本阶段剩余产品修改。达到一批可验收状态时，再统一准备最新 Preview；在 Preview commit 与开发分支 HEAD 一致、相关测试通过之前，不建议合并 main。
