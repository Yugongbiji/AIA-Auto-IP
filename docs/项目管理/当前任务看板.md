# AIA Auto IP 当前任务看板

> 更新时间：2026-08-25
> 规则：这是项目动态入口。新对话 / Codex / 开发者开始前，必须先读“最新有效需求总账 + Owner 清单 + 防回归规则 + 本看板”，不得靠旧聊天或单份 Baseline 猜当前状态。

## 1. 当前阶段

**阶段：PR #12 最新有效需求总账与规则源收口已完成，准备 ECS 本地统一验收。**

- 正式分支：`main`
- 当前开发分支：`agent/ip-product-optimization-20260821`
- 当前 PR：#12《优化 IP 问题、简介服务与内容主线》
- 本轮规则源/Owner/实现修复尚未统一部署 Preview；**旧 Preview 不代表本轮结果。**
- 当前仍不要合并 main。

当前产品主入口：IP 人设、脚本推荐、脚本改写、小红书排版。独立“内容规划”入口已撤销；旧 content plan 只允许历史兼容，不得驱动当前导航、目标或最终业务输出。

## 2. 当前事实源（开发前必读）

按优先级：

1. `docs/product/CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md` —— 当前最高产品事实源；
2. 专项规则（昵称、简介、XHS、内容方向、表达风格等）——只负责其明确细节维度；
3. `docs/product/CURRENT_PRODUCT_RULES_BASELINE_20260824.md` —— 前一版基线快照，只补充未被后续需求覆盖的维度；
4. `rules/product-rule-ownership.md` —— 唯一 Owner；
5. `rules/product-change-governance.md` 与 `docs/项目管理/产品修改防回归规则.md` —— 修改/测试/发布治理；
6. 三轮体验审计与本看板；
7. 当前真实代码。

历史聊天、旧 PR 描述、退役 Vxx、旧专项规则不得覆盖以上事实源。

## 3. 已确认的系统性根因

1. canonical 数据生命周期太晚；
2. Owner 重复/缺失；
3. Prompt 与运行时规则分叉；
4. 退役 Vxx / 独立内容规划泄漏；
5. Preview 环境隔离不完整；
6. 公共 UI 组件过去是假公共化；
7. 测试仍可能保护旧需求；
8. **规则文档本身存在跨版本冲突**：旧 onboarding、目标边界、字段 schema、前端最终集成等文档会把后来需求重新做回去。

## 4. 当前已经完成的关键修复

### 最新有效需求与规则源治理

- [x] 新建 `CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md`，按“最新明确需求覆盖冲突维度”归并历史需求。
- [x] 长期治理规则已升级：最新有效需求总账高于旧 Baseline。
- [x] `product-rule-ownership.md` 与最新 Owner 对齐。
- [x] 独立内容规划两份历史规则明确降级为 `SUPERSEDED / 历史兼容`。
- [x] `rules/前端最终集成与回归规则.md` 删除“V30 必须最后加载”的旧架构，明确 V30/V31/V33 不得复活。
- [x] `rules/ip-onboarding-rules.md` 更新为当前 onboarding：目标二选一、个人介绍展示、纯图标悬浮、无逃逸快捷项。
- [x] `rules/content-goal-boundary-rules.md` 改为最终只认 canonical `primaryGoal`。
- [x] `rules/field-schema.md` 明确 `purpose` 只是 raw source、昵称必须有人物主体、客户反馈/执业证字段边界。
- [x] `rules/dialogue-style-rules.md` 移除独立内容规划和旧 Loading，固定 XHS/脚本公共等待文案。
- [x] `rules/compliance-rules.md` 区分“最低合规允许”与“当前产品更严格生成策略”，避免重新开放友邦/AIA 昵称。
- [x] XHS Emoji 版本链重新核对：以 2026-08-24 的 `rules/xhs-formatting-rules.md V4` 为最新专项规则，任何连续两句至少一个 Emoji。
- [x] `test_current_effective_requirements_contract.py` 已锁上述规则源优先级和退役行为。

### Canonical IP / 76 目标链

- [x] `ip-policy-core.js` 为目标、人群、定位、优势、主线/支线、简介唯一 Owner。
- [x] `/api/generate` 在进入 state/脚本推荐前 canonicalize；匹配用户通过 `/api/proposal/canonical` 回写同版本。
- [x] 模糊 `purpose` 强制拓客/增员二选一；增员使用准增员对象/年龄语义。
- [x] 旧 content plan 不再决定默认页面或目标。

### 昵称 / 简介

- [x] `nickname-policy-v1.js` 补齐真实称呼、原昵称人物锚点、职业/家庭身份、地域、学历层级、荣誉、内容方向与受控 AI fallback。
- [x] 禁止 `成都+称呼 / TOT+称呼 / 985+称呼 / 有温度+称呼` 等机械拼接。
- [x] 简介升级为人物资产池 → 证据排序 → 专业背书/人设记忆/价值服务三策略 → 自动删减 → 平台过滤。
- [x] Prompt 不再生成固定合规尾部；`complianceFooter()` 为唯一 Owner。

### Composer / 推荐脚本 / 公共组件

- [x] `interaction-v2.js` 删除第二套 selection Set 与隐藏 `.multi-confirm.click()` 提交链。
- [x] Composer 只读取 app 业务状态，底部发送为唯一提交动作。
- [x] 推荐脚本补 `rewrite_click / xhs_click` 埋点。
- [x] Clipboard、Loading/Toast、悬浮入口继续保持单一 Owner。

### 小红书

- [x] 原文字词/标点保护与孤立标点修复保留。
- [x] 最新 V4 密度恢复为：平均每 1～2 句话一个视觉锚点，任意连续两句至少一个 Emoji。
- [x] 对应实现、单测、总账、Owner 清单已一致。

## 5. 仍需在 ECS / Preview 验证的内容

这些现在是**验证任务，不再继续扩大产品功能**：

- [ ] 运行 `bash scripts/check-preview-local.sh`，确认规则/单元/回归合同真实通过；
- [ ] 若 ECS 有 Node，确认实际加载 JS `node --check` 通过；
- [ ] 浏览器 smoke：登录/访客 → 76 二选一 → 缺失字段 → 方案 → 浮窗 → 昵称 → 三套简介 → 内容方向 → 推荐脚本；
- [ ] 验证已有资料不重复询问、个人介绍始终最后展示、客户反馈分问题展示；
- [ ] 验证昵称受控策略不机械拼标签，简介三版明显不同且 footer 不重复；
- [ ] 无 IP：L1/L2、两列/一列、真实分页、详情上一篇/下一篇；
- [ ] 有 IP：保险主线/内容支线不重复、换一批正常；
- [ ] 脚本改写：账号表达风格明确进入处理说明与最终表达；
- [ ] 小红书：Loading、复制反馈、任意连续两句 Emoji、孤立标点、原文保护；
- [ ] 移动端：标签不主动拉键盘、自由文本+已选项一次提交、自动滚动、浮窗高度。

## 6. 下一步（当前唯一正确动作）

**进入 ECS 本地统一轻量检查，不再新增产品功能或补新 Vxx。**

```bash
cd /opt/AIA-Auto-IP-preview
git fetch origin
git checkout agent/ip-product-optimization-20260821
git reset --hard origin/agent/ip-product-optimization-20260821
bash scripts/check-preview-local.sh
```

如果全绿，再统一部署 Preview：

```bash
sudo bash scripts/deploy-preview-local.sh agent/ip-product-optimization-20260821
```

检查失败：先读真实错误、定位 Owner/规则源/加载链根因，再一次修复；不盲目部署、不用 GitHub-hosted Actions 碰运气。

## 7. 合并判断

当前：**不建议合并 main。**

还差：ECS 本地门禁全绿 → 新 Preview → 运行态 smoke → 核心业务验收通过 → 再判断是否建议合并 main。
