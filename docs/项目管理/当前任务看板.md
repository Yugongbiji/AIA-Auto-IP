# AIA Auto IP 当前任务看板

> 更新时间：2026-08-25
> 规则：这是项目动态入口。新对话 / Codex / 开发者开始前，必须先读“最新有效需求总账 + Owner 清单 + 防回归规则 + 本看板”，不得靠旧聊天或单份 Baseline 猜当前状态。

## 1. 当前阶段

**阶段：新版 Preview 运行态验收中；86–91 第一批反馈已进入开发分支，简介最新规则已完成“规则源 → Prompt → 唯一 Owner → 测试”收口；同时进入 57 位真实营销员的“预设推荐昵称库”人工验收阶段。昵称验收期间只做规则/样本确认和进度记录，不写昵称数据库，不为每一小批单独部署 Preview。**

- 正式分支：`main`
- 当前开发分支：`agent/ip-product-optimization-20260821`
- 当前 PR：#12《优化 IP 问题、简介服务与内容主线》
- 当前开发分支最新 commit：以 GitHub 分支 HEAD 为准；昵称最高有效规则已同步至 **#114**。
- 当前仍不要合并 main。
- **不要把 10:19 的 56/56 旧门禁误当成当前 HEAD 已通过。** 那次门禁对应上一版 Preview 基线；本轮 86–91、简介规则和昵称规则/验收记录完成后必须在自然节点重新统一执行。
- 当前 ChatGPT 执行环境无法直接 clone GitHub，因此无法在本地实际运行 pytest；这不视为通过，也不视为代码失败，当前状态明确记为“待 ECS 实测”。

当前产品主入口：IP 人设、脚本推荐、脚本改写、小红书排版。独立“内容规划”入口已撤销；旧 content plan 只允许历史兼容，不得驱动当前导航、目标或最终业务输出。

## 2. 当前事实源（开发前必读）

按优先级：

1. `docs/product/CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md` —— 当前最高产品事实源；
2. 专项规则（昵称、简介、XHS、内容方向、表达风格等）——只负责其明确细节维度；
3. `docs/product/CURRENT_PRODUCT_RULES_BASELINE_20260824.md` —— 前一版基线快照，只补充未被后续总账覆盖的维度；
4. `rules/product-rule-ownership.md` —— 唯一 Owner；
5. `rules/product-change-governance.md` 与 `docs/项目管理/产品修改防回归规则.md` —— 修改/测试/发布治理；
6. 三轮体验审计与本看板；
7. 当前真实代码。

历史聊天、旧 PR 描述、退役 Vxx、旧专项规则不得覆盖以上事实源。

## 3. 当前已经完成的治理基础

- [x] 最新有效需求总账已建立，并高于旧 Baseline。
- [x] 每个核心输出唯一 Owner 已收口。
- [x] V30/V31/V33 等退役层不得复活。
- [x] canonical 目标、画像、主线/支线、简介等由 `ip-policy-core.js` 在下游使用前标准化。
- [x] Composer 业务状态只保留一个真源。
- [x] Clipboard、Loading/Toast、悬浮入口、脚本推荐、详情导航等公共组件已明确 Owner。
- [x] XHS Emoji 最新 V4 规则、原文保护和确定性围栏已对齐。

## 4. 本轮 Preview 反馈 86–91

### 86 稳定性 / 防误伤

- [x] 继续执行“最新总账 → 唯一 Owner → 影响范围 → 回归测试 → 自然节点统一 Preview”。
- [x] 不新增 Vxx 后加载补丁，不依赖加载顺序解决业务冲突。
- [x] 本轮修改只落在对应 Owner；未涉及的昵称、主线/支线、推荐数据等不做顺手重构。

### 88 桌面端没有个人资料悬浮入口

- [x] 根因定位为悬浮入口可见性存在 DOM + `state.activeTool` 双状态依赖。
- [x] `profile-float.js` 改为以页面真实可见状态为准，不让 `state.activeTool` 成为第二真源。
- [x] 回归测试已补。
- [ ] 下一版 Preview 动态验证桌面/移动端、会话恢复、从其他工具返回 IP 对话。

### 89 IP 方案提示与板块标题同行

- [x] 最新意见覆盖旧“标题同行 ?”要求。
- [x] V10 改为标题下方/辅助区的次级帮助操作，不把整卡改横排。
- [x] 最高需求总账已同步 `SUPERSEDED` 旧标题同行规则。
- [x] 回归测试已更新。
- [ ] 下一版 Preview 做视觉 smoke。

### 90 合集推荐书名号

- [x] V20 只在展示层把合集名称统一为 `《...》`。
- [x] 不改内容方向本身的业务归类。
- [x] 回归测试已补。

### 91 脚本详情上一篇/下一篇不回顶部

- [x] V17 增加切换新详情后的回顶行为，并对异步渲染做回顶兜底。
- [x] 纳入通用页面切换交互规范。
- [x] 回归测试已补。
- [ ] 下一版 Preview 验证有 IP 推荐和无 IP 脚本库两种列表。

## 5. 简介最新规则与实现（2026-08-25 已确认）

### 规则源

- [x] 原 `docs/简介受控词库与模板-V1-20260824.md` 已原位升级，不新建冲突规则源。
- [x] `CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md` 第 8 节已同步为最高事实。
- [x] 未冲突旧原则继续有效；冲突维度以本次最新确认规则为准。

### 最新核心规则

- [x] 始终回答“我是谁 / 我的优势 / 我能提供什么价值”，每个维度可 1–3 行。
- [x] 不再同一平台给 A/B/C 三套；最终只给小红书 1 套、视频号/抖音共用 1 套。
- [x] 取消机械减法和兴趣/服务/荣誉固定数量上限；真实高价值信息在空间允许时尽量保留。
- [x] 简介使用高度概括、凝练表达，例如“硕士学历”→“硕士”。
- [x] 模糊领域词不得升级成职业经历，例如“法律”不能变成“曾从事法律”。
- [x] 明确法务经历可概括成如“6年企业法务工作经验”。
- [x] 客户评价最终只呈现结论，如 `靠谱｜细致｜有耐心`，不出现“客户高频评价/客户都评价我”等解释前缀。
- [x] 单行必须保持同一语义维度；短信息同维度合并，过长表达压缩，不能为填满一行跨维度硬拼。
- [x] 固定合规尾部仍只有 `complianceFooter()` 一个 Owner。

### Prompt / Owner / 测试

- [x] `prompts/ip-persona-prompt.md` 已删除三套简介、自动减法、1–2 兴趣、3–4 服务等旧指令，改为最新规则。
- [x] `web/ip-policy-core.js` 已删除简介三策略输出，`buildBios()` 每个平台只返回 1 个最终推荐对象。
- [x] 简介新增严格 `bioCareerFacts()`，与 headline 现有 `career()` 分开，避免修简介误伤一句话定位。
- [x] 学历简介表达改为“硕士/博士/985/QS前100”等凝练形式，不再输出“硕士背景”等机械说法。
- [x] 客户评价只作为 `advantage` 结论资产进入简介。
- [x] 使用同维度 `｜` 打包和每维度最多 1–3 行的版面组织。
- [x] `test_current_effective_requirements_contract.py`、`test_ip_policy_contract.py`、`test_regressions_80_85.py` 已取消旧三套简介保护并加入新 contract。
- [ ] ECS 实际执行当前 HEAD 的 contract/regression tests。
- [ ] 用固定真实营销员样本验证自然度、丰富度、单行空间和事实边界。

## 6. 上一版 ECS 门禁记录（历史通过，不代表当前 HEAD）

2026-08-25 10:19 在 `/opt/AIA-Auto-IP-preview` 执行：

```bash
bash scripts/check-preview-local.sh
```

当时结果：56/56 Python contract/regression tests、关键 Python 语法、32 个真实加载 JS 文件存在性、Owner/死链/性能/Preview 环境隔离门禁全部通过。

**本轮已经产生新 commit，因此必须重新跑，不能沿用该结果。**

## 7. 下一步（当前正确动作）

当前优先完成 57 人昵称人工验收；昵称验收期间继续遵守轻量开发流程，不因为每一小批昵称单独部署 Preview。昵称阶段完成后，再与本自然阶段其他修改一起进入统一门禁节点：

1. 继续按真实资料逐批人工验收尚未确认的营销员昵称；每批用户确认后立即更新本看板第 8 节；
2. 57 人全部确认后，先形成最终预设昵称库供产品负责人最终复核；**最终确认前禁止写昵称数据库**；
3. ECS 拉取当前开发分支 HEAD；
4. 运行 `bash scripts/check-preview-local.sh`；
5. 重点运行最新昵称、简介、88–91 contract/regression；
6. 检查实际 `index.html` 加载链与退役 Owner 禁止复活；
7. 门禁全绿后再统一部署下一版 Preview；
8. Preview 继续运行态验收，通过后再判断是否建议合并 main。

**现在仍不建议合并 main，也不建议立即为单个问题或昵称小批次重复部署 Preview。**

## 8. 57 人真实营销员预设昵称人工验收进度

### 真实数据基线

- [x] 已从项目最终名单/客户反馈校正快照重新建立 **57/57** 真实营销员验收基线；名单来源为 `aia_final_roster_peer_reviews_patch_20260824.sql` 对 `agents.survey_json` 的最终同步。
- [x] 该快照记录最终名单 57 人、有效/高可信归属反馈 708 条，并保留 7 条无法可靠归属/明显测试反馈，不把异常反馈硬写给营销员。
- [x] 验收字段以真实资料为准：姓名、现有视频号昵称/账号信息、自我介绍、`peerReviewSummary` 中真实称呼/评价/角色/咨询主题；缺失信息不臆造。
- [x] 当前昵称规则以 `CURRENT_EFFECTIVE_REQUIREMENTS_LEDGER_20260825.md` + `nickname-naturalness-rules-20260825.md` + `nickname-source-ranking-v1.md` + `web/nickname-policy-v1.js` + `prompts/ip-persona-prompt.md` 为准，最高已到 **#114**。

### 历史确认边界的恢复方式

旧看板此前没有逐人记录“57 人昵称验收到哪里”，因此不能把聊天记忆、测试样例或规则示例直接冒充“最终预设昵称已确认”。为避免重复，本轮采用两层状态：

- **规则固化真实样本（最后统一收口的 12 人）**：成涛、宋雨阳、张蕊、贾正佳、郭旭东、袁婷慧（yana）、王一、刘畅、何靖、王帅、任燕、卫梦媛。它们分别直接参与了 #106/#108/#109/#111/#112/#113 的真实边界固化；此前先跳过，避免重复验同一规则边界，但现在需要最后转换为明确最终首选昵称。
- `兔子Nici`、`宽婷Kitty` 等在 #113 中作为中英混合规则示例出现，但只有产品负责人明确确认后才计入最终预设库。
- **只有产品负责人明确回复确认的逐人结果，才计入“最终预设昵称人工确认”数量。** 每确认一批，必须在本节追加姓名、首选昵称和累计进度，换对话后直接从下一位继续。

### #114 已确认优先级结论

- [x] **已有好昵称不机械加修饰语。** #114 是为提升陌生人记忆，不是要求每个人都加形容词；成熟、自然、有特色的原昵称优先保护。
- [x] **宁缺毋滥高于“必须制造记忆点”。** 资料没有足够强、当前成立且自然的特色证据时，不得为了 #114 硬造标签或人设，可退回真实称呼/本名兜底，并允许少于 3–5 个候选。

### 当前检查点

- 真实名单基线：**57/57 已重建**。
- 已完成普通人工验收并明确确认：**45/57**。
- 最后待统一收口：**12 人**——成涛、宋雨阳、张蕊、贾正佳、郭旭东、袁婷慧（yana）、王一、刘畅、何靖、王帅、任燕、卫梦媛。
- 下一步：把上述 12 人按 #114 和最新两条优先级结论转换成明确的最终首选昵称，产品负责人一次性确认后，即完成 57/57 人工验收。

### 数据库 / 发布硬约束

- [ ] 57 人逐人确认完成前，不形成数据库写入脚本。
- [ ] 57 人全部确认后，先汇总最终预设昵称库给产品负责人做一次总确认。
- [ ] **只有最终总确认后，才允许写入昵称数据库。**
- [ ] 昵称每一小批不单独部署 Preview。
- [ ] 当前开发分支继续保持 Draft PR，不合并 `main`。