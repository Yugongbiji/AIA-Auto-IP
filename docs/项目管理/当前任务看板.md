# AIA Auto IP 当前任务看板

> 更新时间：2026-08-24
> 规则：这是项目的动态入口。新对话 / Codex / 开发者开始前，先读“当前产品规则基线 + Owner 清单 + 防回归规则 + 本看板”，不得靠旧聊天猜当前状态。

## 1. 当前阶段

**阶段：PR #12 产品优化收口 + 防回归结构治理。**

- 正式分支：`main`
- 当前开发分支：`agent/ip-product-optimization-20260821`
- 当前 PR：#12《优化 IP 问题、简介服务与内容主线》
- 当前 Preview 最近已部署节点：`4aba805`（旧于当前开发分支，当前结构治理尚未重新部署 Preview）
- 当前开发分支已完成一轮大规模“单一规则源 / 旧规则收权”整理，**尚未完成 ECS 目标测试和统一 Preview，因此不要合并 main。**

当前产品主入口：

1. IP 人设；
2. 脚本推荐；
3. 脚本改写；
4. 小红书排版。

原独立“内容规划”入口已撤销；IP 方案中使用“内容方向”，区分“保险主线 + 内容支线”。

## 2. 当前事实源（开发前必读）

按优先级：

1. `docs/product/CURRENT_PRODUCT_RULES_BASELINE_20260824.md`
2. `rules/product-rule-ownership.md`
3. `rules/product-change-governance.md`
4. `docs/项目管理/产品修改防回归规则.md`
5. 本看板
6. 当前真实代码

历史聊天、旧 PR 描述、旧 Vxx 代码不能覆盖以上当前事实源。

## 3. 当前 P0：先消灭“改好又复发”的结构根因

### 已完成

- [x] 将 082101 + 0824 历史修改记录整理为“当前有效产品规则基线”，按最新规则覆盖冲突维度
- [x] 建立长期产品修改治理规则：需求版本化、Single Source of Truth、Preview 禁止复活检查
- [x] 建立唯一 Owner 清单
- [x] 在项目管理目录新增《产品修改防回归规则》
- [x] README 更新为当前产品事实，不再保留“拓客和增员都要 / 打造个人品牌可作为最终目标”等过期说明
- [x] 停止加载重复/过期层：`product-integration-v30/v31/v33`、`product-rules-v15/v23/v26`
- [x] `ip-policy-core.js` 成为以下最终业务输出唯一 Owner：`primaryGoal`、保险主线、最终内容支线、一句话 IP 定位、简介主体、简介合规尾部
- [x] `nickname-policy-v1.js` 成为推荐昵称唯一 Owner
- [x] `product-rules-v14.js` 只提供内容支线候选与排序，不再直接改最终方案
- [x] `product-rules-v29.js` 只负责客户反馈展示与原昵称审查，不再生成推荐昵称
- [x] `product-rules-v10.js` 只负责首次复制合规 UI，不再生成简介；合规弹窗为“可以说 / 不可以说”，无“返回检查”
- [x] `product-rules-v16.js` 只负责资料缓存，不再追加简介声明/编号
- [x] `product-rules-v27.js` 只负责资料标准化、自我介绍语义提取、客户反馈与个人介绍展示，不再改昵称/悬浮按钮
- [x] `profile-float.js` 收口为悬浮 UI 唯一 Owner：两个纯图标按钮、不显示 V1/V2、只在 IP 对话页展示
- [x] `product-rules-v20.js` 只读取 core 已标准化的保险主线/内容支线进行展示，不再自行重新推断
- [x] `product-rules-v22.js` 收口为 Toast/loading 工具；创作复制真实成功/失败唯一由 V28 负责
- [x] 扩充 `tests/test_ip_policy_contract.py`，锁定多个已确认禁止复发案例

### 当前待完成

- [ ] 在 ECS / Preview 代码目录运行 `tests/test_ip_policy_contract.py`，确认静态契约真实通过
- [ ] 对当前加载 JS 做语法检查，避免结构治理产生基础语法错误
- [ ] 做一次“禁止复活”静态扫描：同一核心字段不得存在第二写入点
- [ ] 定向 smoke：IP 登录 → 资料复用 → primaryGoal 二选一 → 生成方案 → 昵称/简介/内容方向 → 推荐脚本 → 改写/排版
- [ ] 以上通过后统一部署一次 Preview
- [ ] 产品负责人只验收业务效果与内容质量，不再承担基础回归排雷

## 4. 当前重点产品规则（76～78及关联历史项）

### 76：最终目标必须二选一

- [x] 最终 `primaryGoal` 只能为 `customer_acquisition` / `recruitment`
- [x] 报名资料“两者都要 / 打造专业形象 / 个人品牌 / 兼顾”等不能直接成为最终目标
- [x] 对模糊目标必须重新询问：现阶段优先“吸引潜在客户”或“吸引潜在增员对象”
- [ ] Preview 实测

### 77：健康养生不得进入保险主线

- [x] 保险主线使用业务白名单
- [x] 健康养生、育儿、美食、读书、旅行、智能家居、运动等只属于内容支线候选
- [x] 内容支线从过往职业、生活身份、自我介绍、爱好、客户反馈等统一证据池排序
- [ ] Preview 实测：养生不得再进入主线；育儿/其他支线与推荐脚本保持同一标准化值

### 78：简介重构

- [x] 简介主体唯一 Owner
- [x] 简介使用真实个人介绍、过往职业、家庭身份、学历、从业年限、真实荣誉、客户反馈、真实服务字段
- [x] 生活兴趣不得成为简介主内容
- [x] 视频号/抖音合规尾部唯一顺序：个人意见声明 → 营销服务部 → 执业证编号
- [x] `agentId` 不得当执业证编号；真实执业证编号缺失则 `000`
- [x] 旧 V10/V13/V16 等已失去简介写权限
- [ ] Preview 业务质量验收：信息量、自然度、三个版本差异、无重复/标签墙

## 5. 已锁定的禁止复发案例

以下问题一旦修复，后续不得再依赖产品负责人二次发现：

- [x] “无”不能被判为好昵称/建议保留
- [x] QS 前 100 不能推断具体学校
- [x] 推荐昵称必须有且只能有一个称呼
- [x] 成都/TOT/985/有温度等不能机械拼昵称
- [x] 一句话 IP 定位不带称呼
- [x] 健康养生不能进保险主线
- [x] 个人介绍不能被旧层删除
- [x] 客户反馈按问题分组展示
- [x] 执业证编号合规尾部只有一个 Owner
- [x] 营销员编号不能自动当执业证编号
- [x] 悬浮按钮纯图标、无版本号、仅 IP 对话页
- [x] 合规首次复制无“返回检查”
- [x] 创作复制成功提示只由真实复制动作触发
- [ ] 多选后 Enter 可直接发送：需在本次统一 smoke 中再次确认
- [ ] 推荐脚本不得重复/空白：需在本次统一 smoke 中再次确认

## 6. 脚本库当前状态

- 第一批脚本：正式 RDS 原有 199 条
- 2026-08-24 新批次 Word 解析并通过 DMS 成功导入 276 条
- 预计正式脚本库约 475 条；最终以实际数据库查询为准
- 当前 Preview 默认不会自动同步生产 RDS，因此 Preview 脚本数量与正式库可能不同；验收脚本数量时必须先确认 Preview 数据是否已同步

## 7. 开发与验收节奏（长期规则）

**开发优先、阶段验收、一次发布、失败先诊断。**

开发阶段：

- 连续完成同一阶段修改；
- 只跑当前 Owner 和影响范围必要测试；
- 不为每个小修改重复完整 QA / Preview / 正式发布；
- 不默认消耗 GitHub-hosted Actions。

自然验收节点：

1. Owner contract tests；
2. 影响范围 regression tests；
3. 禁止复活扫描；
4. 必要 smoke；
5. 统一一次 Preview；
6. 产品业务验收。

合并 main 前：

- 才跑完整 QA / Frontend QA / 发布门禁；
- 达到合并条件时主动提示“建议合并 main”；
- 必须收到产品负责人明确授权后才能合并。

Actions、Preview、接口或部署失败：先读日志找根因，再一次修复；禁止连续提交试错。

## 8. 下一步（当前唯一正确动作）

**不要继续加产品补丁，也不要马上 Preview。**

下一步先在 ECS/Preview 工作目录执行低成本定向检查：

1. `python -m pytest tests/test_ip_policy_contract.py -q`
2. 当前加载 JS 语法检查
3. 核心 owner / 重复写入扫描

若通过，再进入统一 Preview；若失败，先按真实错误修复，不扩大 scope。

## 9. 合并判断

当前：**不建议合并 main。**

还差：

- 结构治理 contract/regression tests 通过；
- 定向 smoke 通过；
- 新 Preview 部署；
- 76～78 及关联核心项业务验收通过；
- 无阻断性回归。

完成后主动重新判断 PR #12 是否达到可合并状态。
