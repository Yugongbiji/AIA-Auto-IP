# AIA Auto IP 发布收口状态（2026-08-26）

> 用途：明早继续发布时只看当前真实 GitHub 状态，不再从旧聊天猜进度。

## 当前分支与发布状态

- 开发分支：`agent/ip-product-optimization-20260821`
- PR：#12（Draft，未合并 main）
- 当前仍处于发布冻结：只允许发布阻塞级修复，不新增业务需求。
- 最终 HEAD 以 GitHub 当前分支为准；明早一键脚本会先 fetch/reset 到最新 HEAD。

## 今晚已经确认的关键事实

1. ECS 已安装 Node；32 个 `index.html` 实际加载 JS 已做真实 `node --check`，此前 32/32 通过。
2. Preview 浏览器运行时 Owner 均真实存在：`aiaIpPolicy`、`aiaNicknamePolicyV1`、`aiaFloatingUi`、Preview fetch wrapper 均已执行。
3. 悬浮入口问题根因已定位：IP 对话页曾残留 `proposal-screen` 打开状态和 `body.proposal-open`，导致悬浮入口被正确隐藏；已改为进入/切回 IP 对话后按最终 DOM 清掉残留 overlay。浏览器 Console 已验证修复后：`proposal-screen hidden`、`body` 无 `proposal-open`、`floatingDisplay=flex`、`floatingHidden=false`。
4. 121“新姓名/营销员编号仍进入旧历史”存在浏览器旧 session 异步恢复覆盖手工新身份的竞态。Preview 专用 `product-rules-v25.js` 已增加保护：Preview 加载后废弃旧测试 session，并阻止仍在途的旧自动恢复覆盖本次明确输入身份。
5. 简介最终 Owner 仍为 `web/ip-policy-core.js`：历史/新方案都会 canonicalize；正文按“我是谁 / 我的优势 / 我能提供什么价值”三个维度独立组织，12–20 字优选、25 字绝对上限，最终每平台只保留一套推荐简介。`product-rules-v10.js` 只负责最终 UI/复制层兜底和合规提示，不拥有简介业务数据。

## 明早一键收口脚本

已新增：

- `scripts/reset_preview_test_data.py`
- `scripts/finalize_preview_release.sh`
- `tests/test_release_finalization_contract.py`

`check-preview-local.sh` 已纳入上述发布收口 contract，并新增 Python/Shell 语法门禁。

### Preview 清场安全边界

`reset_preview_test_data.py`：

- 只允许 `DB_ENGINE=sqlite`；若不是 SQLite 立即拒绝，防止误碰正式 RDS。
- 只允许在路径含 `preview` 的工作目录执行。
- 清场前用 SQLite backup API 创建时间戳备份。
- 保留 `agents` 原始报名资料、`peer_reviews`、`script_library`。
- 保留 `saved_profiles` 中所有 nickname 相关字段，包括 `nicknamePreset` / 人工备选等。
- 清零：`conversation_messages`、`proposals`、`content_planning_messages`、`content_plans`、`creative_tool_messages`、`script_user_activity`。
- 执行前/后都验证 agents 数量和 57 个 approved nicknamePreset 数量未变化。

## 明早唯一 ECS 操作

在 `/opt/AIA-Auto-IP-preview` 执行：

```bash
cd /opt/AIA-Auto-IP-preview
git fetch origin
git reset --hard origin/agent/ip-product-optimization-20260821
bash scripts/finalize_preview_release.sh
```

脚本会自动：

1. 拉取最新开发分支；
2. 确认 Preview SQLite 隔离；
3. 跑完整发布门禁（含 Node 解析全部实际加载 JS）；
4. 只读预检测试数据和昵称预设；
5. 停 Preview；
6. 备份并清测试历史；
7. 重启 Preview；
8. 检查本机服务、nginx `/preview/`、HEAD 一致性；
9. 输出最终验收信息。

## 明早最后人工验收

一键脚本全绿后，只做最短业务 smoke：

- 用一个干净身份进入，确认不是旧历史对话；
- IP 对话页确认悬浮入口存在；
- 新生成方案只看：昵称、简介短行/同维度、总结 Emoji、右上角 `?`；
- 没有发布阻塞后，再判断 PR #12 是否解除 Draft、合并 main。

在这一步之前不要合并 main，也不要再扩大业务验收范围。
